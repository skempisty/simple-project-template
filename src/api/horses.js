/**
 * @file api/horses.js
 */

/**
 * value = object(username, email, password, user_agent (ua), type, rermote_ip (rip), fname, lname)
 */
exports.get = function() {
    console.log("HORSES RULE!");
};
